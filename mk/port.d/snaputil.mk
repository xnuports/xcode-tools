# snaputil -- APFS snapshot listing and mounting.
#
# A filesystem utility, nothing to do with building code, so it goes to
# usr/local/bin rather than onto the toolchain's parity surface.
P_BUILDSYS=	make
P_NOSTAGE=	yes
P_PROGS=	snaputil
