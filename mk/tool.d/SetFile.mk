# SetFile -- one of the resource tools in src/openxc-tools/Rez.
#
# The directory holds the whole family, so the sources are named rather
# than discovered: without this every tool there would be linked into
# every other one.
T_SRCS=	SetFile.c finderinfo.c
