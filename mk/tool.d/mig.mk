# mig -- the Mach Interface Generator.
#
# A shell script that drives the C preprocessor and then migcom.  It is
# installed as-is; the program it calls is built by mk/tool.d/migcom.mk.
T_SCRIPT=	mig.sh
